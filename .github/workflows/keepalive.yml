name: Keep HF Space Alive

on:
  schedule:
    - cron: "0 0 * * *"   # every day at 00:00 UTC every 12 hours
  workflow_dispatch:  # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Hugging Face Space
        run: curl https://ahmed-ayman-nawy-property-recommender.hf.space/ping
